import React from 'react';

const LoadingState = () => {
    return (
        <h1
            style={{
                height: '75vh',
                display: 'flex',
                justifyContent: 'center',
                alignItems: 'center',
            }}
        >
            Loading State...
        </h1>
    );
};

export default LoadingState;
